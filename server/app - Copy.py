# -*- coding: utf-8 -*-

import os
from flask import Flask
from server.api import api

from search import db

app.config['SECRET_KEY'] = '

class Shop(db.Model):
	id = db.Column(db.Text, primary_key = True) # This should be a UUID